[project]
name = "教学大纲四要点核心内容提取系统"
version = "1.0.0"
debug = true

# LLM 配置已迁移至 .env 文件

[chunking]
chunk_size = 10000
overlap = 1000
batch_size = 100

[logging]
level = "INFO"
format = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
file = ""
